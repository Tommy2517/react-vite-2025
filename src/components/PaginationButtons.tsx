import {FC} from "react";

const PaginationButtons:FC = () => {
    return (
        <div>
            <button>prew</button>
            <button>next</button>
        </div>
    );
};

export default PaginationButtons;